{
  "name": "@anguerjs/http",
  "version": "0.0.3",
  "files": [
    "dist"
  ],
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "rimraf dist && run-s build-bundle build-types",
    "build-bundle": "rollup -c",
    "build-types": "tsc -p tsconfig.type.json --outDir temp && api-extractor run && rimraf temp",
    "rollup-types": "tsc -p tsconfig.type.json --outDir dist",
    "changelog": "conventional-changelog -p angular -i CHANGELOG.md -s --commit-path . --lerna-package http",
    "release": "node ../../scripts/release.js"
  },
  "author": "Anguer",
  "license": "MIT",
  "engines": {
    "node": ">=12.0.0"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/anguer/anguerjs.git",
    "directory": "packages/http"
  },
  "bugs": {
    "url": "https://github.com/anguer/anguerjs/issues"
  },
  "homepage": "https://github.com/anguer/anguerjs/tree/master/packages/http#readme",
  "dependencies": {
    "axios": "^0.21.1",
    "lodash.clonedeep": "^4.5.0"
  },
  "devDependencies": {
    "@types/lodash.clonedeep": "^4.5.6"
  }
}
